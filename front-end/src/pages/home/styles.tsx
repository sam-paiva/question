import { makeStyles } from '@material-ui/core';

const styles = makeStyles({
    root: {
        margin: 0,
        flex: 1,
        padding: 0,
        alignItems: 'center'
    }
});

export default styles;